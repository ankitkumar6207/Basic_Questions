#include<bits/stdc++.h>
using namespace std;
int main(){
    int leap;
    cin>>leap;
    // if((leap % 4 == 0 && leap % 400!=0) || (leap % 100 = 0))
    {
        cout<<"leap year";
    }
    cout<<"not a leap year";
    return 0;
}